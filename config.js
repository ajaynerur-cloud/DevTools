// Where the DevHub API lives. Leave empty when the app is served by server.js (e.g. on Render).
// For GitHub Pages or the Android app, the build workflows set this to your Render URL.
window.DEVHUB_CONFIG = { apiBase: '' };
