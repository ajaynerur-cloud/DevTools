{
  "name": "devhub",
  "version": "3.0.0",
  "private": true,
  "description": "DevHub developer toolkit — web app plus Android wrapper (Capacitor).",
  "scripts": {
    "start": "node server.js",
    "www": "rm -rf www && mkdir www && cp -r index.html styles.css config.js offline-db.js app.js tasks.js worker.js sw.js manifest.webmanifest icons vendor seed www/",
    "android:sync": "npm run www && npx cap sync android",
    "android:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/core": "^8.5.2",
    "@capacitor/filesystem": "^8.1.3",
    "@capacitor/share": "^8.0.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.2"
  }
}
