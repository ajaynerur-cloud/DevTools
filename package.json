{
  "name": "devhub",
  "version": "2.0.0",
  "private": true,
  "description": "DevHub developer toolkit — web app plus Android wrapper (Capacitor).",
  "scripts": {
    "www": "rm -rf www && mkdir www && cp -r index.html styles.css app.js worker.js sw.js manifest.webmanifest icons vendor data www/",
    "android:sync": "npm run www && npx cap sync android",
    "android:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/core": "^8.5.2",
    "@capacitor/filesystem": "^8.1.3",
    "@capacitor/share": "^8.0.2"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^8.5.2"
  }
}
